org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official
# Defaults to the public release. Local SDK validation must override this with
# a unique version so Maven Central cannot satisfy the gate accidentally.
origonSdkVersion=0.3.1
