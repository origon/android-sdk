<?xml version="1.0" encoding="utf-8"?>
<!--
  Pause bars for the audio preview's transport control. Same provenance as
  `ic_play`: no Origon catalog carries one, so it is drawn here (the
  `ic_chevron_left` precedent). Two rounded bars, the catalog's stroke style.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24">
    <path
        android:pathData="M8.25,5.5 V18.5 M15.75,5.5 V18.5"
        android:strokeColor="#FFFFFFFF"
        android:strokeWidth="2.5"
        android:strokeLineCap="round" />
</vector>
